{
  "name": "teslaapps",
  "version": "0.1.0",
  "private": true,
  "description": "Tesla app tooling: a Fleet API mock and client, the TesBids site prototype, and the auction collector.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "mock": "node mock-server/server.js",
    "demo": "node examples/demo.js",
    "test": "node --test test/*.test.js",
    "collect": "node collector/run.js"
  },
  "dependencies": {
    "playwright-core": "^1.56.1"
  }
}
