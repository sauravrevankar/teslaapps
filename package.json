{
  "name": "teslaapps",
  "version": "0.1.0",
  "private": true,
  "description": "Develop Tesla Fleet API apps without owning a Tesla: a local mock server plus a client that works against both the mock and the real API.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "mock": "node mock-server/server.js",
    "demo": "node examples/demo.js",
    "test": "node --test test/*.test.js"
  }
}
